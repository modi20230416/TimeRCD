# Branch 说明：Week 1 - Multi-Scale RCD Fusion

## Branch 名称
`feat/week1-multiscale-rcd`

## 本分支目标
在尽量保持原始 TimeRCD 基线结构不被破坏的前提下，实现第一项增量改进：**多尺度 RCD 融合（Multi-Scale RCD Fusion）**。

该改进的核心思想是：
对同一段时间序列，从不同时间尺度提取上下文表示，并分别计算异常分数，最后进行融合，从而提升模型对不同类型异常（短时突变、局部异常、长期漂移、周期断裂等）的检测能力。

---

## 为什么先做这个改进
相比直接替换主干网络、引入 RCA 模块或 few-shot 适配，多尺度 RCD 融合具有以下优点：

1. 改动范围相对可控
2. 能够复用现有 TimeRCD 主流程
3. 更容易与 baseline 做公平对比
4. 适合作为后续动态归一化、门控融合、RCA 扩展的基础
5. 便于做消融实验，符合毕业设计阶段性推进需求

---

## 本分支要完成的内容

### 1. 保留 baseline 可运行
- 原始 TimeRCD 基线代码必须保持可运行
- baseline 配置与多尺度配置应能通过开关切换
- 不允许因为本分支改动导致 baseline 无法复现

### 2. 增加多尺度输入机制
计划在原始输入基础上增加额外时间尺度，例如：

- 原始尺度 `s1`
- 下采样尺度 `s2`
- 下采样尺度 `s4`

具体下采样方式可先采用简单、稳定的方法，例如：
- 平均池化
- 固定步长采样

第一版优先保证实现清晰、逻辑可复现，不追求复杂自适应策略。

### 3. 各尺度独立计算异常分数
对于每个尺度：
- 尽量复用原始 TimeRCD 的特征提取与异常评分流程
- 输出各尺度对应的 anomaly score
- 保证不同尺度结果可对齐、可比较

### 4. 实现融合机制
本分支至少实现两种融合方式：

#### 方案 A：均值融合
对各尺度分数直接取平均：

```python
final_score = (score_s1 + score_s2 + score_s4) / 3
